import type { PickerInput } from "@/types/picker";

export const DEFAULT_PICKER_INPUT: PickerInput = {
  useCase: "coding",
  budget: "under20",
  billing: "api",
  toolCalls: "yes",
  localOk: "no",
};
